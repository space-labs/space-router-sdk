name: Publish

on:
  push:
    tags:
      - "v*"

jobs:
  test:
    uses: ./.github/workflows/test.yml

  publish-sdk:
    name: Publish Python SDK
    needs: test
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install build
      - run: python -m build
        working-directory: sdk/python
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: sdk/python/dist/
          skip-existing: true

  publish-cli:
    name: Publish CLI
    needs: [test, publish-sdk]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install build
      - run: python -m build
        working-directory: cli
      - uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: cli/dist/
          skip-existing: true

  publish-npm:
    name: Publish JS SDK to npm
    needs: test
    runs-on: ubuntu-latest
    environment: npm
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      # Trusted publishing needs a newer npm than any Node release bundles.
      # Pinned to 11.x: npm 12 requires Node 22.22+, which the runner may not
      # have yet. registry-url is deliberately unset so setup-node does not
      # write an .npmrc pointing at an empty NODE_AUTH_TOKEN, which would
      # shadow the OIDC exchange.
      - run: npm install -g npm@11
      - run: npm --version
      - run: npm install
        working-directory: sdk/js
      - run: npm run build
        working-directory: sdk/js
      - name: Publish (rc tag for prereleases, latest for stable)
        working-directory: sdk/js
        run: |
          NAME=$(node -e "console.log(require('./package.json').name)")
          VERSION=$(node -e "console.log(require('./package.json').version)")
          if [[ "$VERSION" == *-* ]]; then TAG=rc; else TAG=latest; fi
          if npm view "$NAME@$VERSION" version >/dev/null 2>&1; then
            echo "$NAME@$VERSION is already published, skipping"
            exit 0
          fi
          echo "Publishing $VERSION under dist-tag '$TAG'"
          npm publish --provenance --access public --tag "$TAG"

  gh-release:
    name: Create GitHub Release
    needs: [publish-sdk, publish-cli, publish-npm]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          # Full history so --generate-notes can derive the changelog
          # from commits since the previous tag.
          fetch-depth: 0
      - name: Create release (prerelease for tags containing "-")
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="${GITHUB_REF_NAME}"
          PRERELEASE_FLAG=""
          if [[ "$TAG" == *-* ]]; then
            PRERELEASE_FLAG="--prerelease"
          fi
          gh release create "$TAG" \
            --title "$TAG" \
            --generate-notes \
            $PRERELEASE_FLAG
