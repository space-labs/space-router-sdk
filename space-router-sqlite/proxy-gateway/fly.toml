app = "spacerouter-proxy-gateway"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  SR_PROXY_PORT = "8080"
  SR_MANAGEMENT_PORT = "8081"
  SR_LOG_LEVEL = "INFO"
  SR_DEFAULT_RATE_LIMIT_RPM = "60"

[[services]]
  internal_port = 8080
  protocol = "tcp"

  [[services.ports]]
    port = 8080
    handlers = []

  [services.concurrency]
    type = "connections"
    hard_limit = 500
    soft_limit = 400

[[services]]
  internal_port = 8081
  protocol = "tcp"

  [[services.ports]]
    port = 8081
    handlers = ["http"]

  [services.http_checks]
    interval = 10000
    timeout = 2000
    path = "/healthz"
    method = "get"
